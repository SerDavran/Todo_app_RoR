<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>

  <!-- <div class="field"> 
    <%= f.label :state_id %><br>
    <%= f.text_field :state_id %>
  </div> -->

    <div class="field">
    <%= f.label :state_id %><br />
    <%= f.select :state_id, options_for_select(@states.map{ |s| [s.title, s.id] }) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
